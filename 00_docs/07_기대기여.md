# 7. 기대 기여 (Expected Contributions)

1. 필요 전문가 랭크(required expert rank)에서 나타나는 **토큰 수준 이질성(token-level
   heterogeneity)**을 최초로 입증한다.

2. 하나의 중첩(nested) 분해를 사용하는 **동적 prefix 실행**을 도입한다.

3. 라우팅 시점 정보만으로 오라클 연산 할당(computation allocation)을 근사할 수 있음을
   보인다.
