# 9. 리스크 (Risks)

## 리스크 1

전문가(expert)가 충분히 저랭크(low-rank)가 아니다.

→ 동적 실행의 이점이 거의 없다.

## 리스크 2

오라클 랭크 분포가 거의 일정(constant)하다.

→ 정적 랭크(static rank)가 이미 최적이다.

## 리스크 3

라우터 특징(router features)이 오라클 랭크를 예측하지 못한다.

→ 더 강력한 특징(feature)이 필요하다.
